# Never commit the bootstrap key or the connector's identity state.
secret/
agent-state/
qurl-connector-linux-amd64*

# Production native identity and generated account-specific configuration
state-v2/
secret-v2/
runtime/
